# GitHub Pages deploy is disabled. JobGrid is a Next.js app now; ship via Vercel
# once a JobGrid Vercel project is connected to this repo.
name: Deploy

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy is off. Use Vercel for the Next.js app."
